"$schema": "https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json"
name: Site template card container
description: A shell for a site template card, with the screenshot above the details.
group: Card
status: obsolete
props:
  type: object
  properties: {}
slots:
  screenshot_slot:
    title: Screenshot
  details_slot:
    title: Details
  cta_slot:
    title: CTA
