{% set item_classes = 'flex items-center gap-2 text-sm' %}

<div class="flex flex-col">
  {% for item in items %}
    <div class="{{ item_classes }}">
      <span>{{ item }}</span>
    </div>
  {% endfor %}
</div>
